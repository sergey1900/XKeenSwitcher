# XKeenSwitcher

Веб-панель для удобного переключения профилей outbound и маршрутизации (routing) для **XKeen** (Xray / sing-box) на роутерах с установленной средой **Entware** (Keenetic, OpenWrt и др.).

---

## Возможности

- 🚀 Переключение активных профилей в 1 клик с автоматической перезагрузкой службы XKeen.
- 📂 Создание, редактирование, форматирование и валидация конфигураций `outbound.json` и `routing.json` (с поддержкой комментариев `//` и `/* */`).
- 📦 Экспорт и импорт профилей в ZIP / TAR.GZ архивах.
- ⚡ Подсветка синтаксиса JSON в реальном времени.
- 🔄 Мониторинг статуса службы XKeen и просмотр терминального вывода прямо в веб-интерфейсе.
- ⚙️ Гибкая настройка путей к файлам и команд управления через интерфейс.

---

## 📥 Быстрая установка в Entware

Подключитесь к роутеру по SSH и выполните команду:

```bash
curl -sL https://raw.githubusercontent.com/sergey1900/XKeenSwitcher/main/install.sh | sh
```

*(Если репозиторий приватный, скрипт запросит GitHub Token или вы можете передать его через `GITHUB_TOKEN="ваш_токен" sh install.sh`)*

### Что делает скрипт установки:
1. Проверяет наличие среды Entware и необходимых утилит (`node`, `curl`, `tar`, `ca-certificates`). Если чего-то не хватает — автоматически устанавливает через `opkg`.
2. Скачивает и устанавливает файлы приложения в `/opt/etc/xkeen-switcher`.
3. Автоматически находит текущие файлы конфигурации Xray (`05_outbounds.json`, `05_routing.json`, `04_*.json`, `03_*.json` и др.) и создает начальный профиль **Default** (с извлечением адреса сервера в описание).
4. Регистрирует и запускает службу автозапуска `/opt/etc/init.d/S99xkeen-switcher`.
5. Выводит в консоль готовую ссылку для открытия веб-интерфейса в браузере (по умолчанию: `http://192.168.1.1:3000`).

---

## 🗑️ Удаление

Для удаления приложения выполните:

```bash
/opt/etc/xkeen-switcher/uninstall.sh
```
или
```bash
curl -sL https://raw.githubusercontent.com/sergey1900/XKeenSwitcher/main/uninstall.sh | sh
```

Скрипт остановит службу, удалит init-скрипт и запросит раздельное подтверждение на удаление файлов приложения и пакета Node.js.

---

## 🛠️ Ручное управление службой

- **Статус:** `/opt/etc/init.d/S99xkeen-switcher status`
- **Запуск:** `/opt/etc/init.d/S99xkeen-switcher start`
- **Остановка:** `/opt/etc/init.d/S99xkeen-switcher stop`
- **Перезапуск:** `/opt/etc/init.d/S99xkeen-switcher restart`
- **Логи:** `tail -f /opt/var/log/xkeen-switcher.log`
